<!--components/publication_detail/goods/2waited_for_del/2waited_for_del.wxml-->
<view class="waited-del">
  <view class="card" wx:for="{{list}}" wx:key="id">
    <!-- 左侧图片 -->
    <image class="card-img" src="{{item.image || '/images/default-goods.png'}}" mode="aspectFill"></image>
    <!-- 右侧内容 -->
    <view class="card-content">
      <!-- 第一行：名称 -->
      <text class="goods-name">{{item.name}}</text>
      <!-- 第二行：价格 -->
      <text class="goods-price">¥{{item.price}}</text>
      <!-- 第三行：新旧程度、交易方式 -->
      <view class="goods-meta">
        <text class="meta-item" wx:if="{{item.condition}}">{{item.condition}}成新</text>
        <text class="meta-item" wx:if="{{item.tradeType}}">{{item.tradeType}}</text>
      </view>
      <!-- 底部按钮：确认发货 -->
      <view class="card-footer">
        <view class="btn btn-deliver" bindtap="onDeliver" data-id="{{item.id}}">确认发货</view>
      </view>
    </view>
  </view>
  <view wx:if="{{list.length === 0}}" class="empty-state">
    <text>暂无待发货的商品</text>
  </view>
</view>